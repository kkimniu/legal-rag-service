{
	# Let's Encrypt 인증서 발급에 사용할 이메일 (실제 이메일로 변경)
	email admin@YOUR_DOMAIN
}

YOUR_DOMAIN {
	encode gzip

	# API 요청 → 백엔드
	handle /api/* {
		reverse_proxy backend:8000
	}

	# 나머지 → 프론트엔드 (SPA)
	handle {
		reverse_proxy frontend:80
	}
}
